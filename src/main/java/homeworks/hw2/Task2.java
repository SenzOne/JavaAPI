package homeworks.hw2;import java.io.FileWriter;import java.io.IOException;import java.text.SimpleDateFormat;import java.util.Arrays;import java.util.Date;public class Task2 {    public static void main(String[] args) {        int[] arr = new int[]{9, 4, 8, 3, 1};        sort(arr);        int[] arr2 = new int[]{9, 3, 4, 8, 2, 5, 7, 1, 6, 10};        //sort(arr2);    }    public static void sort(int[] array) {        try {            FileWriter in = new FileWriter("log.txt");        } catch (IOException e) {            throw new RuntimeException(e);        }        SimpleDateFormat formatter = new SimpleDateFormat("yyyy-MM-dd HH:mm");        boolean isSorted = false;        while (!isSorted) {            isSorted = true;            for (int i = 1; i < array.length; i++) {                if (array[i] < array[i - 1]) {                    int tmp = array[i];                    array[i] = array[i - 1];                    array[i - 1] = tmp;                    isSorted = false;                }            }            try (FileWriter in = new FileWriter("log.txt", true)) {                Date date = new Date();                //Thread.sleep(2000);                in.append(formatter.format(date)).append(" ").append(Arrays.toString(array)).append("\n");                in.flush();            } catch (IOException e) {                System.out.println("Ошибка" + e);            }            // System.out.println(Arrays.toString(array));        }    }}